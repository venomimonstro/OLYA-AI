services:
  db:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: x1
      POSTGRES_USER: x1
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in .env}
    volumes:
      - x1_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U x1 -d x1"]
      interval: 10s
      timeout: 5s
      retries: 5
    deploy:
      resources:
        limits:
          memory: 1536M

  app:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      X1_DATABASE_AUTO_CREATE_SCHEMA: "false"
      X1_DATABASE_URL: postgresql+psycopg://x1:${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in .env}@db:5432/x1
    ports:
      - "${X1_BIND_ADDRESS:-127.0.0.1}:${X1_PORT:-8000}:8000"
    volumes:
      - x1_data:/app/data
      # Host-visible backups are also visible to deep health checks inside X1.
      - ./backups:/app/backups
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health', timeout=3).read()"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 1024M

  llama:
    profiles: ["inference"]
    image: ghcr.io/ggml-org/llama.cpp:server
    restart: unless-stopped
    volumes:
      - ./models:/models:ro
    command:
      - -m
      - /models/Qwen3.6-35B-A3B-Q4_K_M.gguf
      - --host
      - 0.0.0.0
      - --port
      - "8080"
      - -c
      - "8192"
      - -np
      - "1"
      - --cache-type-k
      - q8_0
      - --cache-type-v
      - q8_0
    expose:
      - "8080"

volumes:
  x1_pgdata:
  x1_data:
